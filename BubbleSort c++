void bubbleSort(int * a, int n)
{
	for (int j = n - 1; j > 0; --j)
	{
		for (int i = 0; i < j; ++i)
		{
			if (a[i] > a[i + 1])
			{
				int toSwap = a[i];
				a[i] = a[i + 1];
				a[i + 1] = toSwap;
			}
		}
	}
}
